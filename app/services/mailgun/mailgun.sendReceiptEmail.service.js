'use strict';

var config = require('../../../config/config'),
    Q = require('q'),
    nodemailer = require('nodemailer'),
    _ = require('lodash'),
    async = require('async');

exports.sendReceiptEmail = function(req, res, next){
  
};
